{
  "spec": {
    "title": "SPARQL 1.2 Protocol",
    "url": "https://w3c.github.io/sparql-protocol/spec/"
  },
  "algorithms": [
    {
      "html": "The following HTTP trace examples illustrate invocation of the <code>query</code> and <code>update</code> operations under several different scenarios. Some example traces are abstracted\n      from complete HTTP traces in these ways:",
      "rationale": "for",
      "steps": [
        {
          "html": "In some examples the string \"<i>EncodedQuery</i>\" represents the URL-encoded string equivalent of the SPARQL query string given in the example; the string \"<i>UnencodedQuery</i>\"\n        represents the exact SPARQL query string given in the example without any encoding."
        },
        {
          "html": "For <code>query</code> operation examples, only partial response bodies, containing the query results, are displayed."
        }
      ]
    }
  ]
}
{
  "spec": {
    "title": "RDF 1.2 Semantics",
    "url": "https://www.w3.org/TR/rdf12-semantics/"
  },
  "algorithms": [
    {
      "name": "substitution mapping",
      "href": "https://www.w3.org/TR/rdf12-semantics/#dfn-substitution-mapping",
      "html": "For RDF terms t, x, and y, where either y is not a triple term or x does not appear in y, we define the <dfn id=\"dfn-substitution-mapping\" tabindex=\"0\" aria-haspopup=\"dialog\" data-dfn-type=\"dfn\">substitution mapping</dfn> t[x/y] inductively, as follows:",
      "rationale": "if",
      "steps": [
        {
          "html": "If t = x, then t[x/y] = y."
        },
        {
          "html": "Otherwise, if t = (s, p, o), then t[x/y] = (s[x/y], p[x/y], o[x/y])."
        },
        {
          "html": "Otherwise, t[x/y] = t."
        }
      ]
    },
    {
      "name": "RDF dataset merge",
      "href": "https://www.w3.org/TR/rdf12-semantics/#dfn-rdf-dataset-merge-0",
      "html": "Given two or more RDF datasets, their <dfn id=\"dfn-rdf-dataset-merge-0\" tabindex=\"0\" aria-haspopup=\"dialog\" data-dfn-type=\"dfn\">RDF dataset merge</dfn> \n            is defined by the following procedure:<br>",
      "rationale": "let",
      "steps": [
        {
          "html": "Force any blank nodes shared between the datasets to be unique in each dataset."
        },
        {
          "html": "Label each triple in the datasets (a) with the name (a URI or a blank node) of the graph that includes it, \n              and (b) with a special label \"default\" if it is included in a default graph."
        },
        {
          "html": "Let the default graph of the <a data-link-type=\"dfn|abstract-op\" href=\"https://www.w3.org/TR/rdf12-semantics/#dfn-rdf-dataset-merge-0\" class=\"internalDFN\" id=\"ref-for-dfn-rdf-dataset-merge-0-1\">RDF dataset merge</a> be the set of\n                triples with the label \"default\"."
        },
        {
          "html": "For each label except the label \"default\", let the name of a named\n                graph in the <a data-link-type=\"dfn|abstract-op\" href=\"https://www.w3.org/TR/rdf12-semantics/#dfn-rdf-dataset-merge-0\" class=\"internalDFN\" id=\"ref-for-dfn-rdf-dataset-merge-0-2\">RDF dataset merge</a> be that label, and let that graph\n                be the set of triples with that label."
        }
      ]
    }
  ]
}